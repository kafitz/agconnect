import update from 'react/lib/update'

const initialState = {}


export function exchangeReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}